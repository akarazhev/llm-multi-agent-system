# 🎭 Запуск Frontend в Mock режиме

## Быстрый старт

```bash
cd frontend-ui
./start-mock.sh
```

Браузер автоматически откроется на `http://localhost:4200`

---

## Что такое Mock режим?

Mock режим позволяет запустить UI **без backend**:
- ✅ Все API запросы перехватываются
- ✅ Возвращаются реалистичные тестовые данные
- ✅ Полностью функциональный интерфейс
- ✅ Отлично для разработки и демо

---

## Доступные данные

### 5 AI Агентов
1. **Business Analyst** - IDLE (15 задач)
2. **Developer** - WORKING (28 задач)  
3. **QA Engineer** - COMPLETED (22 задачи)
4. **DevOps Engineer** - WORKING (19 задач)
5. **Technical Writer** - IDLE (12 задач)

### 6 Workflows
1. **Feature Development** (RUNNING) - JWT Auth API
2. **Bug Fix** (COMPLETED) - WebSocket fix
3. **Infrastructure** (COMPLETED) - K8s setup
4. **Documentation** (COMPLETED) - API docs
5. **Chat Feature** (FAILED) - С ошибкой
6. **Performance Analysis** (COMPLETED) - DB optimization

---

## Альтернативные способы запуска

### NPM команда:
```bash
npm run start:mock
```

### Angular CLI:
```bash
ng serve --configuration=mock --open
```

---

## Что можно тестировать?

✅ Dashboard со статистикой  
✅ Список всех workflows  
✅ Детали каждого workflow  
✅ Обзор агентов  
✅ Навигация между страницами  
✅ Responsive дизайн  
✅ Material Design компоненты  

---

## Технические детали

### Файлы mock системы:
```
src/app/mocks/
├── mock-data.ts          # Все тестовые данные
└── mock.interceptor.ts   # HTTP перехватчик

src/
├── main.mock.ts          # Точка входа для mock
└── environments/
    └── environment.mock.ts
```

### Логирование:
Все перехваченные запросы пишутся в консоль:
```
🎭 [MOCK] GET /api/agents
🎭 [MOCK] GET /api/workflows
```

---

## Troubleshooting

### Порт занят
```bash
ng serve --configuration=mock --port 4300
```

### Зависимости не установлены
```bash
npm install
```

### Скрипт не запускается
```bash
chmod +x start-mock.sh
./start-mock.sh
```

---

## Документация

- `QUICKSTART.md` - Быстрый старт
- `MOCK_MODE.md` - Подробная документация mock режима
- `SETUP.md` - Полная инструкция по установке
- `README.md` - Основная документация

---

**Готово! Наслаждайтесь разработкой! 🚀**
